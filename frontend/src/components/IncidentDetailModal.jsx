function IncidentDetailModal({ incidentId, onClose }) {
  return (
    <div>
      {/* TODO: severity, status change, cause/action/prevention */}
    </div>
  );
}

export default IncidentDetailModal;
